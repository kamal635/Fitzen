import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

/// A class that defines standard border radius values used in the app.
abstract class AppRadius {
  /// General circular border radius used across UI elements.
  static BorderRadius get radiusCircularGeneral => BorderRadius.circular(10.r);
}
